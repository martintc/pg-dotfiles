actions:
  - action: command.run
    command: pg_autoctl
    args:
      - create
      - formation
      - --pgdata
      - /var/lib/postgresql/15/data
      - --kind
      - pgsql
      - --formation
      - formation_two
      - --dbname
      - ecom
